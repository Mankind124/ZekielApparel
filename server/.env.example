# Copy this file to .env and fill in the values for local development.
PORT=3000
JWT_SECRET=generate-a-long-random-string-here
ADMIN_EMAIL=zekielapparel@gmail.com
ADMIN_PASSWORD=ChangeMe2026!
# For Render, DATA_DIR and UPLOADS_DIR are set via render.yaml to /var/data
# Locally, leave these unset — defaults to server/data and server/uploads
