/**
 * API base URL.
 * On Cloudflare Pages everything lives on the same origin as the site,
 * so this is simply an empty string. No configuration needed per environment.
 */
window.ZEKIEL_API_URL = '';
